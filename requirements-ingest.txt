# =============================================================================
# requirements-ingest.txt  —  HEAVY, ingest-only dependencies (opt-in).
#
# Only needed to EXTRACT text from real PDFs at ingestion time. docling pulls in
# large ML deps (torch, etc.), so it is intentionally kept out of the core
# requirements.txt: the retrieval / generation / eval chain imports and runs
# without it, and text_pdf falls back to the `pdftotext` CLI (poppler) when
# docling isn't installed.
#
# Install (only on a machine that ingests documents):
#   pip install -r requirements.txt -r requirements-ingest.txt
# =============================================================================

docling                   # structure-aware PDF extraction (tables, layout)

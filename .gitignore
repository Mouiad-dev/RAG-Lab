# Secrets — the real one. Only .env.example is committed.
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local data / model volumes (should live in docker volumes, never in git)
pgdata/
ollama_data/
uploads/
media/
*.sqlite3

# Editor / OS noise
.idea/
.vscode/
.DS_Store
# ingested documents (don't commit course/book files)
data/*
!data/.gitkeep

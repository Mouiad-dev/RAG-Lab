# RAG Lab — pinned dependencies.
# Rule: a dependency is added the step it is first actually used, and pinned exactly.

# --- Step 0.2: Django skeleton ---
Django==5.2.17          # web framework (5.2 = LTS)
psycopg[binary]==3.3.5  # PostgreSQL driver (needed from Phase 1 for pgvector DB)

# --- Step 1.3: vector storage + search ---
pgvector==0.5.0         # Django VectorField + distance helpers (CosineDistance)

# --- Step 1.5a: typed LLM boundary ---
pydantic==2.12.3        # validated LLM I/O (LLMResponse, price book, cost breakdown)

# --- Step 1.5b: real Ollama adapter ---
httpx==0.28.1           # HTTP client for the Ollama REST API

# --- Step 1.5c: Anthropic (demo LLM) ---
anthropic==1.6.0        # official Anthropic SDK

# --- Step 2.2: async ingestion (task queue) ---
celery==5.4.0           # distributed task queue (Producer/Consumer)
redis==5.2.1            # Redis client — Celery broker + result backend
